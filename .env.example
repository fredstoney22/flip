# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/boilerplate

# Better Auth — generate a strong secret: openssl rand -hex 32
BETTER_AUTH_SECRET=your-secret-here
BETTER_AUTH_URL=http://localhost:5173

# Google OAuth — from Google Cloud Console → Credentials → OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Stripe — from the Stripe dashboard → Developers → API keys
STRIPE_SECRET_KEY=sk_test_your_key_here
# Local dev only — get this by running: npm run stripe:listen
STRIPE_WEBHOOK_SECRET=whsec_your_local_webhook_secret
STRIPE_PRO_PRICE_ID=price_your_price_id_here

# Optional — only when running the standalone Hono dev server (npm run api:dev).
# Leave unset for normal local dev (npm run app:dev) and Vercel production.
# PUBLIC_API_URL=http://localhost:3001
